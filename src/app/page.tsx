import React, { FC } from 'react';
import { ChatWindow } from '../components/chat/chat-window';

const HomePage: FC = () => {
  return (
    <main>
      <ChatWindow />
    </main>
  );
};

export default HomePage;
